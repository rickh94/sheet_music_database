const initialState = {
  list: [],
  isLoading: false,
  errors: {},
  sheet: {}
}

export default function sheets(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
